sonar:
    script: ./build.sh && sonar-scanner -Dsonar.login=$SONARTOKEN -Dsonar.projectKey=flatcraft_2020_$CI_PROJECT_NAMESPACE 
    stage: build
    tags:
     - iut
    artifacts:
     paths:
        - flatcraft.jar
